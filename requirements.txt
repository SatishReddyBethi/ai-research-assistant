langchain_community
pypdf
langchain
langchain-text-splitters
langchain-huggingface
sentence-transformers
langchain-chroma
tqdm
# 'notebook' is Optional. It is for Jupyter Notebook.
# notebook

# Install torch using makefile
# You can also use 'accelerate' package instaed of 'torch' if you just need a lightweight solution for model inference
#torch
# If you have an intel XPU device, you can install the Intel Extension for PyTorch for better performance
# torch==2.8.0
# intel-extension-for-pytorch==2.8.0